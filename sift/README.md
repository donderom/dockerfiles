# [sift](https://sift-tool.org) docker image

The Docker image for the [sift](https://sift-tool.org) based on the lightweight Alpine Linux.

## Usage
```
docker run --rm -it -v $(pwd):/tmp donderom/sift
```
